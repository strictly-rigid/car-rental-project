import React from "react";
import { Navigation } from "../Navigation/Navigation";

const AppBar = () => {
  return (
    <header>
      <div>
        <Navigation />
      </div>
    </header>
  );
};

export default AppBar;
